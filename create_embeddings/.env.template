# Additional Environment Variables for Book Processing
# Add these to your existing .env file

# Database Configuration (if not already present)
POSTGRES_HOST=postgres  # Docker service name for containerized setup
# POSTGRES_DB=your_database_name
# POSTGRES_USER=your_username  
# POSTGRES_PASSWORD=your_password

# Embedding Configuration (if not already present)
# OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=text-embedding-ada-002
PROVIDER=openai

# Processing Configuration (if not already present)
CHUNK_SIZE=500

# Note: Variables commented out with # should already exist in your .env file
# Only add the uncommented ones if they're missing
