# =============================================================================
# DHO Semantic Search System - Miljøvariabel Konfiguration
# =============================================================================
# Kopiér denne fil til .env og udfyld dine værdier
# Alle variabler er påkrævede medmindre andet er angivet

# =============================================================================
# BASIS KONFIGURATION
# =============================================================================
ENVIRONMENT=local                         # miljø: local | test | production

# Database (vælg den linje der passer til dit setup)
POSTGRES_HOST=postgres                    # når kode kører inde i docker compose
# POSTGRES_HOST=localhost                 # når scripts/process_books.sh kører fra værten
POSTGRES_PORT=5432                        # container-port (services inde i compose)
# POSTGRES_PORT=5433                      # standard host-port i docker-compose.base.yml
POSTGRES_DB=dhodb                         # database navn
POSTGRES_USER=postgres                    # database brugernavn
POSTGRES_PASSWORD=your_secure_password    # sikkert database password
DATABASE_URL=postgresql://postgres:your_secure_password@postgres:5432/dhodb  # valgfri men anbefalet

# =============================================================================
# EMBEDDING PROVIDER KONFIGURATION
# =============================================================================
PROVIDER=openai                           # vælg: openai | ollama | dummy
OPENAI_API_KEY=your_openai_api_key        # krævet hvis PROVIDER=openai
OPENAI_MODEL=text-embedding-3-small       # kan ændres til større model
# PROVIDER=ollama
# OLLAMA_BASE_URL=http://ollama:11434     # påkrævet hvis PROVIDER=ollama
# OLLAMA_MODEL=nomic-embed-text           # kan ændres til f.eks. all-minilm
# PROVIDER=dummy                          # kun til tests/CI

# =============================================================================
# TEKST PROCESSERING KONFIGURATION
# =============================================================================
CHUNK_SIZE=500                            # antal tokens/ord per chunk (>0)
CHUNKING_STRATEGY=sentence_splitter       # sentence_splitter | word_overlap
# CHUNKING_STRATEGY=word_overlap          # anvend overlap til lange dokumenter

# =============================================================================
# PROCESSERING KONFIGURATION
# =============================================================================
URL_FILE=test_input.txt                   # Input fil til batch bog processering
LOG_DIR=./logs                            # Directory til log filer

# =============================================================================
# SØGEMASKINE API KONFIGURATION
# =============================================================================
# Kun påkrævet hvis du kører søgemaskine komponenten

TILLADTE_KALDERE=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8080
DISTANCE_THRESHOLD=0.5                    # Minimum similarity score for resultater

# =============================================================================
# VALGFRIE KONFIGURATIONER
# =============================================================================
# Disse variabler har fornuftige defaults men kan tilpasses

# Database Type (default: postgresql)
DATABASE_TYPE=postgresql                  # For fremtidig udvidelse til andre databaser

# Log Level (default: INFO)
LOG_LEVEL=INFO                           # Valg: DEBUG, INFO, WARNING, ERROR
